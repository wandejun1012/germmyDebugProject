今天测下来，发现用多线程还是不用多线程结果是一样的。

我忽然觉得不应该让这2个线程扯上关系。

应该设2个独立线程，再运行看看结果。

main函数：100W条数据只要15秒。。
main函数：100W条数据只要9秒。。

2个多线程跑同一批100W数据，40秒。

2个多线程跑不同批50W数据，39秒。

那么用多线程50W条数据是不是只要7秒？不是的，看上面结果就知道了，难道多线程不能提高
运行效率???

不管啦，头都晕了。

